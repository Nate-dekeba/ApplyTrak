/**
 * Canonical list of job application statuses.
 * Single source of truth — import this anywhere statuses are needed
 * so a rename only requires changing one file.
 */
export const JOB_STATUSES = ['Applied', 'Interviewing', 'Offered', 'Rejected']
