PORT=3000
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
JWT_SECRET=your_jwt_secret_here
# Comma-separated list of allowed frontend origins (CORS). The first entry is also
# used as the canonical app URL for password-reset links and Stripe checkout redirects.
FRONTEND_URL=http://localhost:5173
RESEND_API_KEY=re_your_api_key_here

# Stripe billing
STRIPE_SECRET_KEY=sk_test_key
STRIPE_WEBHOOK_SECRET=whsec_secret
STRIPE_PRICE_MONTHLY=monthly_price_id
STRIPE_PRICE_ANNUAL=annual_price_id
